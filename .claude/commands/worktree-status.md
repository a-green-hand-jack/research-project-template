---
description: Report the state of all active branches and worktrees
---

Use the `branch-reporter` subagent to report on all active branches and
worktrees under `code-worktrees/` and `paper-worktrees/`. It must not push,
merge, or delete anything.
