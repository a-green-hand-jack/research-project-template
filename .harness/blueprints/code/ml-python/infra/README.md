# Infrastructure

Put local, remote, and cluster execution policy here. Migrating to a new compute
environment should require adding or editing `envs/<environment>.yaml`, not
changing source code.

Use `remote-projects.yaml` for shared server mapping and keep private machine
overrides out of Git.
