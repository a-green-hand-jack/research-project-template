# Private Harness Notes

This directory is ignored by default. Use it for local machine notes, private
paths, credentials pointers, and temporary agent state that must not become
durable project memory.

Do not store secrets in Git.
