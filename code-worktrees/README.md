# Code Worktrees

Use this ignored directory as the default sibling root for code worktrees.

Each worktree should have its own `.agent/worktree-status.md` and should share
the project code environment through an absolute `UV_PROJECT_ENVIRONMENT` path
when that policy is enabled.
