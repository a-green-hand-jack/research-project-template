# Experiments

Put runnable experiment logic and configs here. This directory answers "what to
run" and may import from `src/`.

Use `configs/` for reusable experiment settings. Keep raw outputs external or in
ignored paths, and write run pointers to `docs/runs/`.
