# Project-Level Experiment Plans

Use this directory for cross-component experiment plans, ablation matrices, and
claim-evidence plans.

Runnable experiment logic belongs in `code/experiments/` after the code
component is active. Canonical experiment objects and links live in
`memory/boards/experiments.yaml`.
